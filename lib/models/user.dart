class User{
  String name,email,password,phone,age,address;

  User(this.name, this.email, this.password, this.phone, this.age, this.address);
}
class Archives{
   int archive_id;
   String archive_title,archive_url,archive_description;

   Archives(this.archive_id, this.archive_title, this.archive_url,
      this.archive_description);
}